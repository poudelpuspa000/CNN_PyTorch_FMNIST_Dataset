# CNN_PyTorch_FMNIST_Dataset
A comprehensive deep learning framework for image classification using Convolutional Neural Network (CNN) architectures implemented in PyTorch. The system is designed for the Fashion-MNIST dataset and supports flexible execution across CPU, single-GPU, and multi-GPU environments to ensure scalability and computational efficiency.
